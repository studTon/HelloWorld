#include <stdio.h>
main()
{
    int num, i, op;
    do
    {
       puts("Type the option.(0)Exit|(1)Addition|(2)Subtraction|(3)Multiplication|(4)Division");
       scanf("%d", &op);
    } while (op!=0);
    
}
